{
  "version": 2,
  "buildCommand": "uv venv && source .venv/bin/activate && uv pip install -r requirements.txt && python build_football_predictions.py && astro build",
  "outputDirectory": "dist",
  "framework": "astro"
}
